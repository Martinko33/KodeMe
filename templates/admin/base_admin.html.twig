<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Cours video de informatique en ligne, Formation par la video"/>
    <meta name="keywords" content="Language,Developement, HTML,CSS, JavaScript, Python"/>
    <meta name="author" content="Martin"/>
    <title>{% block title %}Page Admin{% endblock title %}</title>
    <link href= "https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('assets/css/admin_style.css') }}">
</head>
<body>
    <header>
        <nav class="d-flex justify-content-between header-sidebar">
            <h1 class="text-center p-5 text-white header-sidebar col-10 col-lg-12">Administration</h1>
            <ul id="hamb-admin">
                <li></li>
                <li></li>
                <li></li>
            </ul>
            {#  slide burger  #}
            <div class="hamburger">
                <nav class="menu">
                    <header>Menu</header>
                    <ol>
                        <li class="respo"><a href="{{ path("display_classes") }}" class="">Classes</a></li>
                        <li class="respo"><a href="{{ path("display_support") }}" class="">Support</a></li>
                        <li class="respo"><a href="{{ path("role") }}" class="">User</a></li>
                        <li class="respo"><a href="{{ path("display_themes") }}" class="">Thémes</a></li>
                        <li class="respo"><a href="#" class="">Evenement</a></li>
                        <li class="respo"><a href="{{ path("page_admin") }}" class="">Home Admin</a></li>
                        <li class="respo"><a href="{{ path("page_home") }}" class="">Home</a></li>
                    </ol>
                </nav>
            </div>
        </nav>
    </header>
<main>
    <section>
        <div>
            {% for message in app.flashes('success') %}
                <div class="flash-notice text-center p-2">
                    {{ message }}
                </div>
            {% endfor %}
        </div>
    </section>
    <nav>
        <div class="sidebar">
            <a href="{{ path("display_classes") }}" class="link text">Classes</a>
            <a href="{{ path("display_support") }}" class="link text">Support</a>
            <a href="{{ path("role") }}" class="link text">User</a>
            <a href="{{ path("display_themes") }}" class="link text">Thémes</a>
            <a href="#" class="link text">Evenement</a>
            <a href="{{ path("page_admin") }}" class="link text">Home Admin</a>
            <a href="{{ path("page_home") }}" class="link text">Home</a>
        </div>
    </nav>
    <div>
        {% block header_admin %}
        {% endblock header_admin %}
    </div>
    <div>
        {% block main %}
        {% endblock main %}
    </div>

</main>
<footer></footer>
    <script src="{{ asset('assets/js/admin_script.js') }}"></script>
    {% block script_video %}{% endblock script_video %}

</body>
</html>