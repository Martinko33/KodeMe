<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Cours video de informatique en ligne, Formation par la video"/>
    <meta name="keywords" content="Language,Developement, HTML,CSS, JavaScript, Python"/>
    <meta name="author" content="Martin"/>
    <title>{% block title %}Cours en ligne{% endblock title %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
</head>
<body>


<header {% block barHeader %}{% endblock barHeader %}>
    <nav class="bar-navigation">
        <ul class="lientop">
                <li class="responsive box-bar"><a href="{{ path("page_cours") }}">Cours</a></li>
                <li class="responsive box-bar"><a href="{{ path("construct_page") }}">Evenement</a></li>

                <li class="box-bar1"><a href="{{ path("page_home") }}" class="logo">KodeMe</a></li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="responsive box-bar"><a href="{{ path("page_admin") }}">Admin</a></li>
                    {% else %}
                    <li class="box-bar"></li>
                {% endif %}

                {% if app.user %}
                    <li class="responsive box-bar"><a href="{{ path("app_logout") }}">{{app.user.firstname}} {{ app.user.name}} <i class="bi bi-box-arrow-in-right fs-2"></i></a></li>
                {% else %}
                    <li class="responsive box-bar"><a href="{{ path("app_login") }}">Connexion <i class="bi bi-person-circle fs-2"></i></a></li>
                {% endif %}
        </ul>
        {#  pour responsive      #}
        <ul id="hamb">
            <li></li>
            <li></li>
            <li></li>
        </ul>
        
        <div class="hamburger">
            <nav class="menu">
                <header>Menu</header>
                <ol>
                    <li class="respo"><a href="{{ path("page_cours") }}">Cours</a></li>
                    <li class="respo"><a href="{{ path("construct_page") }}">Evenement</a></li>

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="respo"><a href="{{ path("page_admin") }}">Admin</a></li>
                    {% endif %}

                    <li><a href="#">Contact</a></li>

                    {% if app.user %}
                        <li class="respo"><a href="{{ path("app_logout") }}">Déconnexion</a></li>
                    {% else %}
                        <li class="respo"><a href="{{ path("app_login") }}">Connexion </a></li>
                    {% endif %}


                </ol>

            </nav>
        </div>
    </nav>
    {% block noticeText %}
    {% endblock noticeText %}
</header>
<main>
    {% block search %}
    {% endblock search %}
    <div>
        {% for message in app.flashes('success') %}
            <div class="flash-notice text-center p-2">
                {{ message }}
            </div>
        {% endfor %}
    </div>

    {% block main %}
        {% block carrousel %}
        {% endblock carrousel %}
    {% endblock main %}

</main>


<footer class="bar-navigation">
        <div class="">
            <ul>
                <li><h3>Contact</h3></li>
                <li><a href="{{ path("page_home") }}"></a>KodeMe</li>
                <li><a href="#"></a>+33 7 77 77 77 77</li>
                <li><a href="#"></a>info@kodeme.fr</li>
            </ul>
        </div>
        <div class="">
            <ul>
                <li><h3>Cours</h3></li>
                <li><a href="{{ path("page_cours") }}">Online cours</a></li>
                <li><a href="#"> Cours les enfants</a></li>
                <li><a href="#"> Pour école</a></li>
                <li><a href="#"> Proffeseur</a></li>
                <li><a href="#"> Deviens prof</a></li>
            </ul>
        </div>
        <div class="">
            <ul>
                <li><h3>Info</h3></li>
                <li><a href="#"> A propo de nous</a></li>
                <li><a href="#"> FAQ</a></li>
                <li><a href="#"> Références</a></li>
                <li><a href="#"> Blog</a></li>
                <li><a href="#"> Nous contacte</a></li>
            </ul>
        </div>
</footer>
<script src="{{ asset('assets/js/script.js') }}"></script>
{% block script_carrousel %}{% endblock script_carrousel %}
{% block script_video %}{% endblock script_video %}


</body>
</html>